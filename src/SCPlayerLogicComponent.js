// The player's logic. Update every frame to see what the player should do based on state of itself and other things like touch
var SCPlayerLogicComponent = cc.Class.extend({

	ctor:function () {
    	cc.log("SCPlayerLogicComponent ctor()");
    	
    },
    
    update:function (){
	    
	    
    }

});